
import 'dart:convert';

import 'package:doan/model/models/vouchermodel.dart';
import 'package:flutter/services.dart';

class ReadData{
  // Future<List<Voucher>> loadData() async{
  //   var data = await rootBundle.loadString("assets/files/voucherlist.json");
  //   var dataJson = jsonDecode(data);

  //   return (dataJson['data'] as List).map((e) => Voucher.fromJson(e)).toList();
  // }
}